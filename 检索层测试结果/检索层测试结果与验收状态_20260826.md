---
title: 检索层测试结果与验收状态
date: 2026-08-26
tags: [RAG, 检索层, 测试, 验收]
---

# 检索层测试结果与验收状态

## 自动化测试

2026-08-26 执行：

```powershell
.\.build-env\Scripts\python.exe -m unittest discover -s tests -v
```

结果：

```text
Ran 103 tests in 35.157s

OK
```

| 范围 | 数量 | 结果 |
|---|---:|---:|
| 输入层 | 25 | PASS |
| 索引层 | 26 | PASS |
| 检索层 | 52 | PASS |
| 合计 | 103 | **PASS** |

## 当前检索层重点覆盖

- exact 关键词补回向量 Top K 外记录；
- exact/keyword 候选可越过向量 0.40 门槛；
- 加权 RRF 排序与去重；
- 整页 PDF 视觉不作为最终区域证据；
- 显式 previous/next 链路的有界跨页扩展；
- `adjacent_context`、`context_pages`、`evidence_blocks` 契约；
- Studio 显示证据角色、模态、位置和跨页范围；
- 视觉图片安全路径校验。

## 历史真实模型报告

三个 20260824 子目录记录了旧排序策略的真实模型基线。最佳历史结果为 Recall@5/10 均 100%、MRR 0.5856，但当时尚无 BM25、exact、当前 RRF 和证据块扩展。

因此这些文件不修改、不删除，但只作为历史比较。当前版本的正式语义质量状态是：

| 项目 | 状态 |
|---|---|
| 程序与边界测试 | 103/103 PASS |
| 当前混合搜索真实 Recall/MRR | **待重新验收** |
| 跨页证据块人工核对 | **待正式验收** |
| LLM 答案质量 | 未进入本层范围 |

## 下一步验收条件

使用同一 30 问题集重新运行当前版本，保存新的 JSON 和 Markdown；对比检索层内部候选搜索模块中每个问题的向量、关键词、exact 贡献，以及后续 evidence block 内容。在新报告生成前，不把旧版 0.5856 MRR 标注为当前成绩。
